import 'package:flutter/material.dart';

class SendMenuItems {
  String? title;
  IconData? icons;
  MaterialColor? color;
  SendMenuItems(
      {required this.title, required this.icons, required this.color});
}
